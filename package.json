{
  "name": "teb-enterprises",
  "private": true,
  "scripts": {
    "api": "cd backend && .venv\\Scripts\\python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "website": "cd website && npm run dev -- --host 127.0.0.1 --port 5173",
    "admin": "cd admin && npm run dev -- --host 127.0.0.1 --port 5174"
  }
}
